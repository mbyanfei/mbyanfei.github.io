window.AGENT_DEEP={"m1": {"title": "What Is an Agent? — Deep Dive", "why": "You should be able to define an agent without using a framework name and show the exact trust boundary between model decisions and software execution.", "mental": ["Agentic behavior is about control-flow authority. The model can choose among actions based on goal, context, and intermediate state.", "Autonomy is a spectrum. A production agent might only choose one approved tool or route; it does not need unrestricted planning.", "Agent runtime responsibilities include tool catalog, state, validation, policy, execution, termination, observability, and recovery. The LLM is one component.", "Ask 'what decision is the model making?' If you cannot point to a real dynamic decision, the system may simply be an LLM-powered workflow."], "walkTitle": "Walkthrough: model decision vs trusted runtime", "walkCode": "decision = llm_decide(\n    goal=user_request,\n    state=state,\n    allowed_actions=[\"search_policy\", \"query_balance\", \"ask_user\"]\n)\n\nif decision.action == \"query_balance\":\n    args = validate(decision.args)\n    authorize(user, \"query_balance\", args.customer_id)\n    result = balance_api.get(args.customer_id)\n    state.add_observation(result)\n", "steps": ["`llm_decide` is probabilistic and advisory.", "`allowed_actions` bounds what the model can even propose.", "`validate` makes the tool contract enforceable.", "`authorize` is a trusted policy decision outside the model.", "The result becomes verified state for later reasoning."], "runTitle": "Runnable mini-example: tiny stateful router", "runCode": "def decide(text):\n    t = text.lower()\n    if \"balance\" in t:\n        return \"query_balance\"\n    if \"policy\" in t:\n        return \"search_policy\"\n    return \"ask_user\"\n\nfor q in [\"show balance\", \"find policy 709\", \"help me\"]:\n    print(q, \"->\", decide(q))\n", "run": "Standard library only. This is deterministic on purpose—the point is to see the control-flow boundary before replacing the router with an LLM.", "expected": "Each request routes to one bounded action.", "drillQ": "If I removed LangGraph, would this still be an agent?", "outline": ["Yes, if the model still dynamically chooses actions/control flow.", "Frameworks provide orchestration primitives; they do not define agency.", "Explain exactly what the model decides and what deterministic controls remain around it."], "pitfalls": ["Defining 'agent' as 'LangGraph app'.", "Conflating internal chain-of-thought with externally authorized action."]}, "m2": {"title": "Why Agent? — Deep Dive", "why": "This is an architecture judgment question. Senior interviewers care whether you can avoid unnecessary probabilistic complexity.", "mental": ["Start from the simplest deterministic system that could solve the task. Only add LLM decisions where fixed rules are brittle or cannot cover the task space economically.", "Good agentic zones: semantic interpretation, ambiguous intent, dynamic evidence gathering, adaptive tool selection, open-ended research. Bad zones: exact calculations, permissions, settlement logic, schema enforcement, irreversible writes.", "Quantify the incremental value: task coverage, manual steps removed, cycle time, quality, or user adoption. Compare cost/latency/failure rate against the baseline.", "Hybrid architecture is normal: deterministic outer workflow with bounded agentic nodes."], "walkTitle": "Walkthrough: controlled agentic architecture", "walkCode": "intent = llm_extract_intent(request)     # semantic ambiguity\nvalidated = validate(intent)               # deterministic\nauthorized = authorize(user, validated)    # deterministic\n\nif validated.kind == \"known_report\":\n    result = run_report(validated)         # fixed workflow\nelse:\n    plan = llm_choose_tool(validated)       # bounded agentic choice\n    result = run_approved_tool(plan)        # deterministic execution\n\nanswer = llm_explain(result)                # semantic presentation\n", "steps": ["The model handles language ambiguity first.", "Validation and authorization stay deterministic.", "Known tasks take the cheaper fixed path.", "Only unknown/dynamic tasks pay for agentic planning.", "Final explanation can be probabilistic because it does not change source-of-truth calculations."], "runTitle": "Runnable mini-example: baseline comparison table", "runCode": "baseline = {\"success\": .82, \"latency_s\": 1.2, \"cost\": .01}\nagent = {\"success\": .93, \"latency_s\": 4.8, \"cost\": .09}\n\nfor k in baseline:\n    print(k, \"baseline=\", baseline[k], \"agent=\", agent[k])\n", "run": "Standard library only. Replace with your measured numbers in a real experiment.", "expected": "The trade-off becomes explicit: quality improved, but latency and cost also increased.", "drillQ": "Why is this an agent at all?", "outline": ["Name the specific dynamic decision.", "Explain why fixed logic was insufficient or too costly to maintain.", "Identify deterministic boundaries.", "Give evidence the added complexity improved business/task metrics."], "pitfalls": ["Choosing agentic architecture because it feels more advanced.", "Making the entire workflow probabilistic when only one step needs semantics."]}, "m3": {"title": "Tool Calling — Deep Dive", "why": "Tool calling is one of the most likely technical interview areas because it exposes whether you understand schema contracts, security, retries, and runtime boundaries.", "mental": ["The model emits a *proposal*: tool name + arguments. Your code owns parsing, validation, authorization, execution, timeout, retry, result normalization, and logging.", "Schema validation catches structural errors; semantic validation catches business-invalid values; authorization catches permission violations. All three are different.", "Tool errors should be classified into repairable argument error, transient dependency error, permanent business/policy error, and unknown/system error. The recovery differs by class.", "Never let untrusted tool output become executable instructions. Treat tool results as data, preserve provenance, and constrain what subsequent model calls can do."], "walkTitle": "Code walkthrough: complete validated tool boundary", "walkCode": "from dataclasses import dataclass\n\n@dataclass\nclass BalanceArgs:\n    customer_id: str\n\ndef parse_args(raw):\n    cid = raw.get(\"customer_id\")\n    if not isinstance(cid, str) or not cid.strip():     # 1\n        raise ValueError(\"customer_id is required\")\n    return BalanceArgs(cid.strip())\n\ndef run_balance_tool(user, raw):\n    args = parse_args(raw)                              # 2\n    if args.customer_id not in user.allowed_customers: # 3\n        raise PermissionError(\"not entitled\")\n\n    try:\n        return balance_api(args.customer_id, timeout=3) # 4\n    except TimeoutError:\n        raise RuntimeError(\"dependency timeout\")        # 5\n", "steps": ["Validate the shape/type of model-produced arguments.", "Convert raw model output into a trusted internal type.", "Perform resource-level authorization with trusted entitlement data.", "Execute with an explicit timeout.", "Translate dependency failures into controlled application errors; the orchestration layer decides whether retry/fallback is appropriate."], "runTitle": "Runnable mini-example: simulate valid, invalid, and unauthorized calls", "runCode": "class User:\n    allowed_customers = {\"C1\", \"C2\"}\n\ndef balance_api(customer_id, timeout):\n    return {\"customer_id\": customer_id, \"balance\": 125.50}\n\nfor raw in [\n    {\"customer_id\": \"C1\"},\n    {\"customer_id\": \"\"},\n    {\"customer_id\": \"C9\"},\n]:\n    try:\n        print(run_balance_tool(User(), raw))\n    except Exception as e:\n        print(type(e).__name__, e)\n", "run": "Standard library only.", "expected": "C1 succeeds; empty ID fails validation; C9 fails authorization.", "drillQ": "What if the model calls the right tool with the wrong but schema-valid customer ID?", "outline": ["Schema validation is insufficient.", "Apply semantic/business validation and resource-level authorization.", "Use user context/entitlements from trusted systems.", "Log denied attempts and do not send restricted resource details back to the model."], "pitfalls": ["Using prompt instructions as validation.", "Assuming schema-valid means business-valid."]}, "m4": {"title": "State, Checkpoints & Recovery — Deep Dive", "why": "State is what lets an agent survive multi-step work, human pauses, failures, retries, and restarts without losing correctness.", "mental": ["Keep state explicit and minimal: run ID, identities, normalized inputs, current node, verified tool outputs, decisions, checkpoint/version metadata. Do not persist arbitrary hidden reasoning.", "Checkpoint at boundaries where repeating work is expensive, slow, external, or unsafe. Checkpointing after every token is unnecessary; never checkpointing makes recovery expensive.", "Resume is only safe if prior side effects are known. Use idempotency keys, transaction state, or compensation for writes.", "Version compatibility matters: if workflow/schema/tool versions changed, an old checkpoint may require migration or restart."], "walkTitle": "Code walkthrough: durable checkpoint with SQLite", "walkCode": "import sqlite3, json\n\ndb = sqlite3.connect(\"state.db\")\ndb.execute(\"create table if not exists checkpoints(run_id text primary key, payload text)\")\n\ndef save(run_id, state):\n    with db:\n        db.execute(\n          \"insert or replace into checkpoints values(?,?)\",\n          (run_id, json.dumps(state))\n        )\n\ndef load(run_id):\n    row = db.execute(\n      \"select payload from checkpoints where run_id=?\",\n      (run_id,)\n    ).fetchone()\n    return json.loads(row[0]) if row else None\n", "steps": ["State is serialized into a durable store rather than process memory.", "`run_id` is the recovery key and should also appear in traces/logs.", "`insert or replace` makes checkpoint updates idempotent for a run.", "Load returns the last durable state after restart."], "runTitle": "Runnable mini-example: simulate crash + resume", "runCode": "state = {\"node\": \"retrieve\", \"results\": [], \"version\": \"v1\"}\nsave(\"R1\", state)\n\n# pretend the process crashed here\nrestored = load(\"R1\")\nrestored[\"node\"] = \"analyze\"\nrestored[\"results\"].append({\"doc\": \"D7\"})\nsave(\"R1\", restored)\n\nprint(load(\"R1\"))\n", "run": "Standard library only.", "expected": "Final state shows node `analyze`, one result, and version `v1`.", "drillQ": "Step 3 fails after steps 1 and 2 succeeded. Restart from the beginning?", "outline": ["Depends on replay safety and checkpoint semantics.", "Read-only deterministic steps may be replayed.", "Expensive steps can resume from checkpoint.", "Side effects require idempotency/commit status/compensation.", "Validate workflow version before resuming."], "pitfalls": ["Saying 'the framework handles checkpoints' without explaining semantics.", "Persisting full sensitive transcripts by default."]}, "m5": {"title": "Reliability & Idempotency — Deep Dive", "why": "This topic exposes whether you understand distributed-system ambiguity: timeout does not mean failure, and retry does not mean safety.", "mental": ["Classify errors first. Invalid request and permission denial are permanent; network reset, 429, and temporary 503 may be transient. Retry only the latter and only when operation semantics allow it.", "A timeout is ambiguous: the server might have committed the write but the response was lost. For non-idempotent operations, blind retry can duplicate business actions.", "Use idempotency keys for write APIs, durable job IDs for async work, and deduplication on the receiver. For multi-step business transactions, compensation may be needed.", "Bound retries with backoff + jitter and a retry budget. Otherwise failures amplify into retry storms."], "walkTitle": "Code walkthrough: retry with backoff, jitter, and safety flag", "walkCode": "import random, time\n\ndef retry(fn, *, safe_to_repeat, attempts=3):\n    for i in range(attempts):\n        try:\n            return fn()\n        except (TimeoutError, ConnectionError):\n            if not safe_to_repeat:\n                raise\n            if i == attempts - 1:\n                raise\n            time.sleep((2 ** i) + random.random())\n", "steps": ["Only known transient exceptions are caught.", "`safe_to_repeat` makes operation semantics explicit.", "Final attempt re-raises instead of hiding failure.", "Exponential delay reduces load; jitter prevents synchronized retry spikes."], "runTitle": "Runnable mini-example: transient failure succeeds on third attempt", "runCode": "attempt = 0\n\ndef flaky_read():\n    global attempt\n    attempt += 1\n    print(\"attempt\", attempt)\n    if attempt < 3:\n        raise TimeoutError()\n    return \"OK\"\n\nprint(retry(flaky_read, safe_to_repeat=True))\n", "run": "Standard library only.", "expected": "Two timeouts followed by `OK` on attempt 3.", "drillQ": "Would you retry a payment API after timeout?", "outline": ["Not blindly.", "Determine whether API supports idempotency key/status lookup.", "Timeout may have occurred after server commit.", "Use same idempotency key on retry or reconcile status first.", "If no safe contract exists, escalate/reconcile rather than risk duplicate payment."], "pitfalls": ["Retrying every exception.", "Assuming POST is never idempotent or GET is always safe—actual API semantics matter."]}, "m6": {"title": "RAG & Retrieval Quality — Deep Dive", "why": "A senior Agent Engineer still needs to reason about evidence quality because agent decisions can be only as good as the retrieved context.", "mental": ["Decompose RAG into ingestion, retrieval, ranking, context construction, and generation. Each has distinct metrics and failure modes.", "Use hybrid retrieval when queries mix exact enterprise language with conceptual phrasing. Push ACL and hard metadata filters into retrieval.", "Reranking spends more compute after broad candidate retrieval to improve precision. It is often a better next step than immediately changing the embedding model.", "Freshness/provenance are operational concerns: source versions, effective dates, reindex triggers, citation IDs, and stale-document handling."], "walkTitle": "Code walkthrough: retrieval pipeline with explicit gates", "walkCode": "def retrieve(user, query):\n    lexical = bm25_search(query, k=20)\n    semantic = vector_search(query, k=20)\n    candidates = rrf(lexical, semantic)        # combine rankings\n\n    candidates = [d for d in candidates\n                  if can_read(user, d)]        # entitlement gate\n\n    ranked = rerank(query, candidates)\n    return ranked[:6]\n", "steps": ["Two retrieval modes protect exact and semantic recall.", "Rank fusion combines them without assuming scores share a scale.", "Authorization occurs before context construction.", "Reranking improves final precision before the model sees evidence."], "runTitle": "Runnable mini-example: RRF ranking", "runCode": "def rrf(*lists, k=60):\n    score = {}\n    for xs in lists:\n        for i, doc in enumerate(xs, 1):\n            score[doc] = score.get(doc, 0) + 1/(k+i)\n    return sorted(score, key=score.get, reverse=True)\n\nprint(rrf([\"D1\",\"D3\",\"D7\"], [\"D3\",\"D2\",\"D1\"]))\n", "run": "Standard library only.", "expected": "D3/D1 rank above documents appearing in only one list.", "drillQ": "RAG answer quality dropped. What is your diagnostic order?", "outline": ["Check expected evidence exists in source/index.", "Inspect parsing/chunking and freshness.", "Evaluate raw retrieval Top-K and filters/ACL.", "Inspect reranking/context assembly.", "Only then inspect generation/prompt/model."], "pitfalls": ["Changing the LLM before examining retrieval.", "Treating vector database choice as the main RAG quality lever."]}, "m7": {"title": "Evaluation — Deep Dive", "why": "Agent evaluation needs to score behavior, not just final prose. A system can produce a plausible answer after choosing the wrong tool or violating process rules.", "mental": ["Use layered evals: tool selection, tool arguments, retrieval, state transitions, final answer, latency/cost, and business task success.", "Build deterministic evaluators for structured behavior wherever possible. Example: tool == `get_balance`, customer_id == expected, no forbidden tool calls, amount within tolerance.", "Use LLM judges for semantic qualities but calibrate with humans and stable rubrics. Store judge model/prompt versions too.", "Run regression suites on every meaningful prompt/model/tool/index change and slice by failure type/risk."], "walkTitle": "Code walkthrough: agent behavior evaluator", "walkCode": "def score_run(run, expected):\n    return {\n      \"tool_ok\": run[\"tool\"] == expected[\"tool\"],\n      \"args_ok\": run[\"args\"] == expected[\"args\"],\n      \"answer_ok\": expected[\"answer\"] in run[\"answer\"],\n      \"too_many_steps\": run[\"steps\"] > expected[\"max_steps\"],\n    }\n\nexpected = {\n  \"tool\": \"get_balance\",\n  \"args\": {\"customer_id\": \"C7\"},\n  \"answer\": \"100\",\n  \"max_steps\": 3,\n}\n", "steps": ["Score the action independently of the final answer.", "Exact argument checks catch wrong-resource behavior.", "Answer correctness can be a separate deterministic or rubric-based metric.", "Step-count bounds expose looping/inefficiency."], "runTitle": "Runnable mini-example: compare good vs plausible-but-wrong run", "runCode": "good = {\"tool\":\"get_balance\",\"args\":{\"customer_id\":\"C7\"},\n        \"answer\":\"Balance is 100\",\"steps\":2}\nbad = {\"tool\":\"search_docs\",\"args\":{\"customer_id\":\"C7\"},\n       \"answer\":\"Balance may be 100\",\"steps\":5}\n\nprint(score_run(good, expected))\nprint(score_run(bad, expected))\n", "run": "Standard library only.", "expected": "Good run passes behavior checks. Bad run may look semantically plausible but fails tool and step metrics.", "drillQ": "How do you know a prompt change improved the agent?", "outline": ["Versioned eval set and baseline.", "Layered metrics so you know what changed.", "Risk slices and regression gates.", "Latency/cost comparison.", "Production online metrics after release."], "pitfalls": ["Evaluating only final answer text.", "Changing eval rubric at the same time as the candidate system."]}, "m8": {"title": "Observability — Deep Dive", "why": "Without trace-level observability, production agent debugging becomes storytelling. You need to reconstruct the exact run.", "mental": ["Use a single trace/run ID across API, orchestrator, retrieval, model, tool, queue, and downstream service calls.", "Capture component versions and structured metadata: model, prompt version, index, tool schema, tokens, latency, tool name, status, retry count, evidence IDs.", "Logs answer 'what event happened'; metrics answer 'how often/how much'; traces answer 'what happened in this one request'.", "Protect telemetry: redact secrets/PII, avoid logging hidden reasoning, use role-based access and retention rules."], "walkTitle": "Code walkthrough: lightweight tracing with context manager", "walkCode": "from contextlib import contextmanager\nimport time\n\n@contextmanager\ndef span(name, **meta):\n    start = time.perf_counter()\n    try:\n        yield\n        status = \"ok\"\n    except Exception:\n        status = \"error\"\n        raise\n    finally:\n        ms = (time.perf_counter()-start)*1000\n        print({\"span\": name, \"status\": status,\n               \"latency_ms\": round(ms,2), **meta})\n", "steps": ["Span starts a timer and carries structured metadata.", "Exceptions propagate—the tracer should not hide failures.", "`finally` records status/latency whether success or error.", "In production the record goes to telemetry with the shared trace ID."], "runTitle": "Runnable mini-example: instrument a tool call", "runCode": "with span(\"tool_call\", trace_id=\"R42\", tool=\"balance\", model=\"A\"):\n    time.sleep(0.05)\n    result = {\"balance\": 100}\n\nprint(result)\n", "run": "Standard library only.", "expected": "A structured tool-call record with roughly 50 ms latency, then the result.", "drillQ": "A user says the agent gave the wrong answer yesterday. What data do you need?", "outline": ["Trace/run ID or user/time to locate it.", "Prompt/model/index/tool-schema versions.", "Retrieved evidence IDs and tool calls/arguments/results.", "State transitions and policy outcomes.", "Relevant errors/retries/latency.", "Enough sanitized input/output to reproduce without exposing unnecessary sensitive data."], "pitfalls": ["Logging full sensitive prompts by default.", "Having model-provider traces that are not correlated with application/tool traces."]}, "m9": {"title": "Security, Identity & Governance — Deep Dive", "why": "Agent security is software security plus a new untrusted decision-maker. The safe design assumes the model can be manipulated or wrong.", "mental": ["Identity path: authenticate caller → resolve user/session claims → determine entitlements → authorize each sensitive resource/action → execute with least-privilege workload identity.", "Service identity and user identity are not interchangeable. A service credential authenticates the workload; it does not automatically prove which human is entitled to which data.", "Prompt injection should be expected. Contain it by limiting tools, validating arguments, filtering retrieved content, separating instructions/data, approval gates, and policy enforcement outside the LLM.", "Audit should link user, service identity, tool, resource, policy outcome, trace ID, and relevant business result."], "walkTitle": "Code walkthrough: user + service identity separation", "walkCode": "def get_balance(user, customer_id):\n    # User authorization\n    if customer_id not in entitlement_service.allowed_customers(user):\n        raise PermissionError()\n\n    # Service authenticates to downstream API\n    token = workload_identity.get_access_token(\n        audience=\"balance-api\"\n    )\n\n    return balance_api.get(\n        customer_id,\n        bearer_token=token\n    )\n", "steps": ["Authorization decision is based on the human/user context.", "Downstream authentication uses a workload token scoped to the service.", "The model is absent from the permission decision.", "Audit can record both user and workload identities."], "runTitle": "Runnable mini-example: policy gate", "runCode": "ALLOWED = {\"alice\": {\"C1\", \"C2\"}}\n\ndef authorize(user, customer):\n    if customer not in ALLOWED.get(user, set()):\n        raise PermissionError(f\"{user} cannot access {customer}\")\n\nfor c in [\"C1\", \"C9\"]:\n    try:\n        authorize(\"alice\", c)\n        print(c, \"allowed\")\n    except PermissionError as e:\n        print(c, \"denied:\", e)\n", "run": "Standard library only.", "expected": "C1 allowed; C9 denied.", "drillQ": "If the backend uses one service account to access SQL Server, how do you preserve end-user authorization?", "outline": ["Authenticate the user at the application boundary.", "Resolve user entitlements in trusted policy/data layer.", "Enforce row/resource access before/while querying.", "Use service account only for workload authentication, with least privilege.", "Propagate user/trace context for audit; use delegated identity if downstream must enforce user identity itself."], "pitfalls": ["Treating a service account as equivalent to user authorization.", "Relying on model refusal to protect restricted data."]}, "m10": {"title": "Context Engineering — Deep Dive", "why": "Context quality often matters more than clever prompting. Senior design is about what the model sees, when, why, and with what trust level.", "mental": ["Separate authoritative context (policy, tool results, systems of record) from conversational/untrusted context (user claims, retrieved text, memory). The model should know provenance.", "Use node-specific context. Planning needs goals/tool schemas; tool execution does not need full chat history; answer synthesis needs verified results/citations.", "Summaries and long-term memory are lossy caches, not systems of record. Re-fetch authoritative values when correctness matters.", "Manage context budget actively: deduplicate, rank evidence, compress low-value history, preserve high-authority instructions and relevant source snippets."], "walkTitle": "Code walkthrough: context builder with authority tiers", "walkCode": "def build_context(step, state):\n    common = [\n      {\"role\":\"system\", \"kind\":\"policy\",\n       \"text\": SYSTEM_POLICY}\n    ]\n\n    if step == \"plan\":\n        return common + [\n          {\"kind\":\"user_request\", \"text\": state.request},\n          {\"kind\":\"tool_catalog\", \"text\": tool_catalog()},\n        ]\n\n    if step == \"respond\":\n        return common + [\n          {\"kind\":\"verified_result\", \"text\": state.result},\n          {\"kind\":\"citations\", \"text\": state.citations},\n        ]\n", "steps": ["System policy is always included as high-authority context.", "Planner sees request and tools—only what it needs to select an action.", "Responder sees verified results and citations rather than raw operational clutter.", "Structured `kind` labels make provenance explicit and easier to inspect."], "runTitle": "Runnable mini-example: token-budget-like truncation", "runCode": "def keep_within_budget(items, max_chars):\n    out, used = [], 0\n    for item in items:\n        if used + len(item) > max_chars:\n            break\n        out.append(item)\n        used += len(item)\n    return out\n\nevidence = [\"important policy\", \"supporting example\", \"low value history\"]\nprint(keep_within_budget(evidence, 35))\n", "run": "Standard library only.", "expected": "Only the first items fitting the budget remain. Real systems rank by value before truncating.", "drillQ": "What is the difference between prompt engineering and context engineering?", "outline": ["Prompt engineering focuses on instructions/wording.", "Context engineering decides the complete information environment: policy, state, memory, retrieval, tool results, structured data, provenance, and budget.", "In production, the latter controls correctness, freshness, and cost across multiple model calls."], "pitfalls": ["Sending the entire conversation to every node.", "Letting stale memory override systems-of-record data."]}};
