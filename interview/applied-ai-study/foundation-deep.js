window.FOUNDATION_DEEP={"python": {"title": "Python Engineering — Deep Dive", "why": "You do not need to memorize obscure Python syntax. You need to show that you can write code that is readable, testable, memory-conscious, failure-aware, and suitable for a service or data pipeline.", "mental": ["Think in layers: pure transformation functions in the middle, I/O at the edges. Pure functions are easier to test because the same input gives the same output and they do not hide network/database side effects.", "Use generators/iterators when you can process records incrementally. A list materializes every item in memory; a generator yields one item at a time. This matters for large ingestion jobs.", "Typing, dataclasses/Pydantic models, logging, configuration, tests, and explicit exception boundaries are not ceremony. They make contracts visible and failures diagnosable.", "Concurrency choice follows workload: async I/O for many waits on network/files, threads for blocking I/O libraries, multiprocessing for CPU-bound Python work. Do not say 'async is faster' without naming the bottleneck."], "walkTitle": "Code walkthrough: streaming + validation + explicit error boundary", "walkCode": "from dataclasses import dataclass\nfrom typing import Iterable\n\n@dataclass(frozen=True)\nclass Txn:\n    customer_id: str\n    amount: float\n\ndef parse_rows(rows: Iterable[dict]):\n    for row in rows:                         # 1\n        try:\n            amount = float(row[\"amount\"])    # 2\n            if amount == 0:\n                continue                     # 3\n            yield Txn(row[\"customer_id\"], amount)  # 4\n        except (KeyError, ValueError) as exc:\n            raise ValueError(f\"bad row: {row}\") from exc  # 5\n\ndef total_outflow(rows: Iterable[dict]) -> float:\n    return sum(-t.amount for t in parse_rows(rows) if t.amount < 0)  # 6\n", "steps": ["1. `rows` is any iterable, so the caller can pass a list, file reader, database cursor, or streaming API response.", "2. Parsing happens at the boundary. Convert untrusted strings to typed values once instead of spreading conversions throughout the business logic.", "3. Business filtering is explicit and cheap. No intermediate dataframe is required just to skip zero amounts.", "4. `yield` makes `parse_rows` a generator. Only the current transaction needs to exist in memory.", "5. Catch only errors you understand and re-raise with useful context. `except Exception: pass` destroys diagnosability.", "6. The aggregation consumes the generator lazily. For a very large source, memory usage stays roughly constant."], "runTitle": "Runnable mini-example: process one million rows without building a million-object list", "runCode": "from itertools import islice\n\ndef rows(n):\n    for i in range(n):\n        yield {\"customer_id\": f\"C{i%100}\", \"amount\": \"-2.50\" if i % 3 == 0 else \"1.00\"}\n\nsample = list(islice(rows(5), 5))\nprint(sample)\nprint(total_outflow(rows(1_000_000)))\n", "run": "Save the walkthrough and mini-example in one file and run `python demo.py`. No external packages are required.", "expected": "The program prints five sample rows and an outflow close to 833,335.00, while the source itself is generated lazily.", "drillQ": "How would you process 30 million records without exhausting memory?", "outline": ["Start with streaming/partitioned reads instead of loading everything into a dataframe.", "Use generators or database cursors; transform in bounded batches if vectorized operations are needed.", "Write incremental outputs/checkpoints rather than retaining all results.", "Measure the real bottleneck: memory, network, CPU, or serialization.", "Add retry/idempotency at I/O boundaries and tests around pure transformations."], "pitfalls": ["Saying generators are always faster. Their main advantage is lazy memory behavior; per-item Python loops may be slower than vectorized batch operations.", "Using pandas for every problem. For large workloads, push filters/aggregations to SQL or use streaming/columnar engines where appropriate."]}, "ingestion": {"title": "Enterprise Data Ingestion — Deep Dive", "why": "Interviewers are testing whether you can turn 'connect to another source' into a reusable capability rather than another one-off script.", "mental": ["Separate source-specific acquisition from reusable processing. A connector knows how to read Salesforce/S3/API/SQL; the common pipeline knows how to validate, land, parse, normalize, quality-check, publish, and observe.", "Land immutable raw data first when replay/audit matters. That gives you a recovery point independent of the source and protects you from parser/transformation bugs.", "Idempotency means rerunning the same ingestion unit does not create duplicate business results. Common keys are source + object + source version, or source + date partition + run identifier.", "Schema evolution needs policy: compatible additions may flow through; breaking type/removal changes should quarantine or version the contract rather than silently corrupt downstream consumers."], "walkTitle": "Code walkthrough: idempotent ingestion with SQLite", "walkCode": "import sqlite3\nfrom hashlib import sha256\nimport json\n\ndb = sqlite3.connect(\"ingest.db\")\ndb.execute(\"\"\"\ncreate table if not exists raw_events(\n  event_key text primary key,\n  source text not null,\n  payload text not null\n)\n\"\"\")\n\ndef event_key(source: str, record: dict) -> str:\n    canonical = json.dumps(record, sort_keys=True).encode()\n    return sha256(source.encode() + b\"|\" + canonical).hexdigest()\n\ndef ingest(source: str, records: list[dict]) -> int:\n    inserted = 0\n    with db:                                           # 1\n        for r in records:\n            key = event_key(source, r)                 # 2\n            cur = db.execute(\n                \"insert or ignore into raw_events values(?,?,?)\",\n                (key, source, json.dumps(r))\n            )                                          # 3\n            inserted += cur.rowcount\n    return inserted                                    # 4\n", "steps": ["`with db:` creates a transaction boundary: either the batch commits or the context rolls back on an exception.", "The deterministic event key represents the identity of the source record/version. Replaying identical content generates the same key.", "`insert or ignore` plus a primary key turns duplicate delivery into a no-op rather than duplicate rows.", "The function returns inserted count, which is a useful operational metric. A real pipeline would also count received, rejected, quarantined, and published records."], "runTitle": "Runnable mini-example: prove replay safety", "runCode": "batch = [\n    {\"id\": 1, \"name\": \"A\"},\n    {\"id\": 2, \"name\": \"B\"},\n]\n\nprint(\"first run:\", ingest(\"demo_api\", batch))\nprint(\"replay:\", ingest(\"demo_api\", batch))\nprint(\"rows:\", db.execute(\"select count(*) from raw_events\").fetchone()[0])\n", "run": "Run twice. Standard library only.", "expected": "First run inserts 2; replay inserts 0; row count remains 2.", "drillQ": "How do you make onboarding the twentieth source cheaper than onboarding the first?", "outline": ["Define a canonical source/record contract and connector interface.", "Reuse landing, metadata, validation, quality, observability, retry, lineage, and publishing stages.", "Make source-specific code configuration-driven where possible, but do not force fundamentally different source semantics into one abstraction.", "Provide templates/tests and clear onboarding SLAs.", "Track per-source operational metrics and schema versions."], "pitfalls": ["Equating idempotency with 'delete then reload.' That can be destructive and breaks concurrent consumers.", "Building a universal connector abstraction so generic that every source still needs custom escape hatches."]}, "docintel": {"title": "Document Intelligence — Deep Dive", "why": "For enterprise RAG, parsing quality is upstream model quality. A PDF that 'successfully parsed' can still lose tables, headings, reading order, or footnotes and poison retrieval.", "mental": ["First classify the document: native digital PDF, scanned PDF, Word, HTML, spreadsheet, slide deck. The extraction strategy should follow the document type.", "Preserve structure as a canonical representation: page, section, heading hierarchy, paragraph, table, list, figure/caption, footnote, metadata. Flattening everything into one text blob discards signals retrieval could use.", "OCR is a fallback for images/scans, not a reflex. Native text extraction is usually cheaper and more accurate for digital PDFs.", "Evaluate parsers on a gold set with structural criteria: text completeness, table fidelity, reading order, heading recovery, page/source provenance."], "walkTitle": "Code walkthrough: canonical blocks rather than one text string", "walkCode": "from dataclasses import dataclass\n\n@dataclass\nclass Block:\n    page: int\n    kind: str        # heading | paragraph | table | footnote\n    text: str\n    section: str | None = None\n\nblocks = [\n    Block(1, \"heading\", \"Liquidity Risk\", \"3\"),\n    Block(1, \"paragraph\", \"The bank maintains...\", \"3\"),\n    Block(2, \"table\", \"Metric | Limit\\nLCR | >=100%\", \"3.1\"),\n]\n\n# downstream chunking can preserve section + block type\nfor b in blocks:\n    print({\"page\": b.page, \"kind\": b.kind,\n           \"section\": b.section, \"text\": b.text})\n", "steps": ["A block model preserves layout semantics in structured fields instead of hiding them in text.", "Page and section provenance can later become citation metadata.", "Tables can be serialized separately or represented row-wise; the important point is not to merge them blindly with neighboring prose.", "Chunking can now respect section boundaries and choose different strategies by block kind."], "runTitle": "Runnable mini-example: validate parser output coverage", "runCode": "expected_kinds = {\"heading\", \"paragraph\", \"table\"}\nactual_kinds = {b.kind for b in blocks}\nmissing = expected_kinds - actual_kinds\nprint(\"missing block types:\", missing)\nassert not missing\n", "run": "Run with the block definitions above.", "expected": "No missing block types. In a real gold-set test, assertions would compare expected headings/tables/text spans against parser output.", "drillQ": "How would you evaluate two PDF parsers?", "outline": ["Choose representative documents, especially tables, scans, multi-column layouts, footnotes, and bad scans.", "Define structural gold expectations rather than eyeballing a few outputs.", "Measure content recall, reading-order accuracy, table fidelity, metadata/provenance, latency, and cost.", "Compare downstream retrieval impact as well as parser metrics.", "Keep parser version in lineage so a bad release can be reprocessed."], "pitfalls": ["Using OCR on every PDF.", "Evaluating parser quality only by whether extraction returned non-empty text."]}, "chunking": {"title": "Chunking — Deep Dive", "why": "Chunking controls the unit retrieval sees. Bad chunking can make an excellent embedding model look weak.", "mental": ["Chunk size is not a universal constant. It depends on document structure, question granularity, retrieval model, and context budget.", "Small chunks can increase precision but fragment definitions and tables. Large chunks preserve context but inject noise and waste tokens.", "Structure-aware chunking usually beats blind fixed windows for enterprise documents: keep headings with paragraphs, avoid splitting tables, preserve section IDs, and use overlap only when it solves boundary loss.", "Parent-child retrieval is useful when you want small units for accurate matching but larger parent sections for answer context."], "walkTitle": "Code walkthrough: simple structure-aware chunker", "walkCode": "def chunk_sections(sections, max_chars=220):\n    chunks = []\n    for section_id, heading, paragraphs in sections:\n        current = heading\n        for p in paragraphs:\n            candidate = current + \"\\n\" + p\n            if len(candidate) > max_chars and current != heading:\n                chunks.append({\n                    \"section\": section_id,\n                    \"text\": current\n                })\n                current = heading + \"\\n\" + p\n            else:\n                current = candidate\n        if current:\n            chunks.append({\"section\": section_id, \"text\": current})\n    return chunks\n", "steps": ["The chunker iterates by logical section first. It never mixes unrelated sections just to reach a token target.", "The heading is carried into every chunk, which gives retrieval additional context.", "The size cap is a safety boundary, not the only chunking rule.", "Metadata (`section`) is emitted alongside text for filtering, provenance, and parent lookup."], "runTitle": "Runnable mini-example: see how section boundaries are preserved", "runCode": "sections = [\n (\"1\", \"Authentication\", [\n   \"Authentication verifies identity.\",\n   \"Authorization determines permitted actions.\",\n ]),\n (\"2\", \"Retries\", [\n   \"Retry only transient failures.\",\n   \"Writes need idempotency before blind retry.\",\n ])\n]\n\nfor c in chunk_sections(sections, max_chars=90):\n    print(c, \"\\n\")\n", "run": "Standard library only.", "expected": "Authentication and retry content remain in separate chunks; headings are repeated with their own paragraphs.", "drillQ": "How would you choose chunking for policy documents versus financial reports?", "outline": ["Policy: section/article-aware chunks, preserve effective date and policy ID, parent-child around subsections.", "Financial reports: respect headings and tables; tables may need row/column-aware representation plus surrounding narrative.", "Create representative query sets and compare Recall@K/answer quality across chunking variants.", "Optimize jointly with reranking/context budget rather than chunk size in isolation."], "pitfalls": ["Giving a single magic number such as '500 tokens' as the answer.", "Adding large overlap everywhere; overlap increases index size, redundancy, and duplicate evidence."]}, "retrieval": {"title": "Retrieval & AI Search — Deep Dive", "why": "Retrieval is where exact enterprise identifiers and fuzzy semantic intent collide. Senior candidates should know why hybrid retrieval and reranking exist.", "mental": ["Lexical/BM25 retrieval is strong for exact strings, codes, product names, error IDs, and rare terminology. Dense retrieval is strong when wording differs but meaning is similar.", "Hybrid retrieval combines both candidate sets. You then normalize/merge scores or use rank fusion so one score scale does not dominate the other.", "Metadata filters should enforce hard constraints such as entitlement, document type, date, business line, or jurisdiction before sensitive content enters the model context.", "Reranking is a second-stage precision step: retrieve 20–100 cheap candidates, then use a stronger cross-encoder/LLM/ranker on a smaller set."], "walkTitle": "Code walkthrough: Reciprocal Rank Fusion (RRF)", "walkCode": "def rrf(*ranked_lists, k=60):\n    scores = {}\n    for ranked in ranked_lists:\n        for rank, doc_id in enumerate(ranked, start=1):\n            scores[doc_id] = scores.get(doc_id, 0) + 1/(k + rank)\n    return sorted(scores, key=scores.get, reverse=True)\n\nbm25 = [\"D3\", \"D1\", \"D8\", \"D2\"]\nvector = [\"D1\", \"D7\", \"D3\", \"D9\"]\n\nprint(rrf(bm25, vector))\n", "steps": ["Each retriever returns a ranking, not necessarily comparable raw scores.", "RRF awards points based on rank position, avoiding direct score normalization.", "A document appearing high in both lists accumulates more score.", "The fused ranking becomes the candidate set for filters/reranking."], "runTitle": "Runnable mini-example: combine exact + semantic candidates", "runCode": "fused = rrf(\n    [\"policy-709\", \"pricing-overview\", \"mmda-faq\"],\n    [\"pricing-overview\", \"policy-709\", \"deposit-products\"]\n)\nprint(fused[:3])\n", "run": "Standard library only.", "expected": "Documents that rank well across both methods rise to the top.", "drillQ": "Vector retrieval quality is poor. What do you inspect first?", "outline": ["Verify parsing/chunking and whether the needed text actually exists in the index.", "Inspect query wording and exact identifiers; add lexical/hybrid retrieval when appropriate.", "Check metadata filters/ACLs for over-filtering.", "Review embedding model/domain mismatch only after upstream pipeline issues.", "Use retrieval evals (Recall@K etc.) before changing the generation model."], "pitfalls": ["Blaming embeddings before verifying parser/chunk quality.", "Applying authorization after retrieval/context construction."]}, "rag": {"title": "RAG Architecture — Deep Dive", "why": "A senior RAG answer should separate the offline indexing system from the online query system and show where security, evaluation, versioning, and citations live.", "mental": ["Offline/indexing path transforms governed source content into retrievable units: acquisition → parse → normalize → chunk → metadata/ACL → embed/index → version/publish.", "Online/query path transforms a user question into evidence: authenticate → query transform → retrieve → entitlement filter → rerank → context assembly → generation → citation/grounding checks.", "Version everything that can change retrieval behavior: parser, chunker, embedding model, index build, query transform, reranker, prompt/model. Otherwise regression diagnosis becomes guesswork.", "Do not treat RAG as a hallucination cure. If evidence is missing, stale, unauthorized, or contradicted, the system should abstain/clarify rather than confidently answer."], "walkTitle": "Code walkthrough: minimal end-to-end RAG control flow", "walkCode": "def answer_question(user, query):\n    candidates = hybrid_search(query, k=30)          # 1\n    allowed = [d for d in candidates\n               if can_read(user, d)]                 # 2\n    ranked = rerank(query, allowed)[:6]               # 3\n    context = build_context(ranked, max_chars=12000)  # 4\n\n    if not ranked or ranked[0].score < MIN_SCORE:     # 5\n        return {\"answer\": \"I need more evidence.\",\n                \"citations\": []}\n\n    answer = generate(query, context)                  # 6\n    return {\"answer\": answer,\n            \"citations\": [d.source for d in ranked]}  # 7\n", "steps": ["Retrieve broadly enough to protect recall.", "Apply authorization before evidence enters model context. In real systems it is better to push this filter into retrieval itself.", "Rerank for precision.", "Context assembly controls order, duplication, token budget, and source labels.", "Abstention is a feature. A minimum-evidence rule prevents the model from treating empty retrieval as permission to improvise.", "Generation uses evidence rather than replacing retrieval logic.", "Citations should point to stable source identities/versions, not merely display titles."], "runTitle": "Runnable mini-example: tiny local RAG without an external LLM", "runCode": "docs = {\n \"auth\": \"Authentication verifies identity. Authorization checks permission.\",\n \"retry\": \"Retry transient failures. Writes require idempotency.\",\n \"rag\": \"RAG retrieves evidence before generation.\"\n}\n\ndef search(q):\n    terms = set(q.lower().split())\n    scored = []\n    for k, text in docs.items():\n        score = len(terms & set(text.lower().replace(\".\",\"\").split()))\n        scored.append((score, k, text))\n    return sorted(scored, reverse=True)\n\nq = \"What checks permission?\"\nbest = search(q)[0]\nprint(\"evidence:\", best[2])\nprint(\"answer:\", \"Authorization checks permission.\" if best[0] else \"Need more evidence.\")\n", "run": "Standard library only. This intentionally replaces generation with a deterministic answer to isolate the retrieval mechanism.", "expected": "The `auth` document is selected as evidence and the answer is grounded in that evidence.", "drillQ": "How do you distinguish retrieval failure from generation failure?", "outline": ["Inspect whether the expected evidence appears in Top-K before looking at final prose.", "If evidence is absent: parsing/chunking/query/index/filter/ranking problem.", "If evidence is present but answer is wrong: context assembly/model/prompt/instruction-following problem.", "Maintain separate retrieval and generation evals so the distinction is measurable."], "pitfalls": ["Tuning the LLM when expected evidence never reaches the context.", "Returning citations that were retrieved but did not actually support the claim."]}, "evaluation": {"title": "AI/RAG Evaluation — Deep Dive", "why": "Evaluation is what turns AI iteration from demos and opinions into engineering. You need a dataset, metrics, failure taxonomy, and release decision.", "mental": ["Build a representative gold set from real task distributions, not only happy paths. Include common, ambiguous, adversarial, out-of-domain, permission-sensitive, and high-risk cases.", "Separate layers: retrieval metrics tell you whether evidence was found; generation metrics tell you whether the model used it correctly; agent metrics tell you whether actions/tools were correct; business metrics tell you whether the system created value.", "Use deterministic assertions whenever possible (schema valid, correct tool, exact ID, numeric tolerance). Use LLM-as-judge for nuanced rubrics such as explanation quality, but calibrate against humans.", "Evaluate changes by slices/failure categories, not only one average. A model can improve overall while getting worse on a small high-risk class."], "walkTitle": "Code walkthrough: small regression harness", "walkCode": "CASES = [\n {\"q\": \"2+2?\", \"expected\": \"4\", \"risk\": \"low\"},\n {\"q\": \"customer C7 balance?\", \"expected\": \"100\", \"risk\": \"high\"},\n]\n\ndef evaluate(system):\n    rows = []\n    for c in CASES:\n        got = system(c[\"q\"])\n        ok = got.strip() == c[\"expected\"]\n        rows.append({**c, \"got\": got, \"ok\": ok})\n    return rows\n\nrows = evaluate(my_system)\nsuccess = sum(r[\"ok\"] for r in rows) / len(rows)\nhigh_risk_ok = all(r[\"ok\"] for r in rows if r[\"risk\"] == \"high\")\n\nassert success >= 0.90\nassert high_risk_ok\n", "steps": ["Cases encode expected behavior and relevant slices such as risk.", "The evaluator records outputs rather than only one aggregate score, enabling failure analysis.", "Overall task success is useful but insufficient.", "A separate high-risk gate prevents a small dangerous regression from hiding behind the average."], "runTitle": "Runnable mini-example: compare two candidate systems", "runCode": "def v1(q):\n    return {\"2+2?\": \"4\", \"customer C7 balance?\": \"100\"}[q]\n\ndef v2(q):\n    return {\"2+2?\": \"4\", \"customer C7 balance?\": \"unknown\"}[q]\n\nfor name, fn in [(\"v1\", v1), (\"v2\", v2)]:\n    rows = evaluate(fn)\n    print(name, [(r[\"q\"], r[\"ok\"]) for r in rows])\n", "run": "Standard library only.", "expected": "v1 passes both cases; v2 exposes a high-risk regression even though half the test set still passes.", "drillQ": "What would you measure before replacing an embedding model?", "outline": ["Freeze a representative retrieval test set with expected evidence.", "Compare Recall@K/NDCG plus latency/index size/cost.", "Run end-to-end task eval because retrieval metric improvement may not improve answers.", "Slice by document type/query type/business line.", "Define a release threshold before running the experiment."], "pitfalls": ["Building the eval set after seeing candidate results.", "Using LLM-as-judge as the only source of truth for numeric/tool/security correctness."]}, "agents": {"title": "AI Agents — Deep Dive", "why": "The key is not loops or frameworks. It is deciding where probabilistic control adds value and surrounding it with deterministic contracts.", "mental": ["An agent loop usually has goal/context → model decision → validated action → execution → observation/state update → repeat or finish.", "Tools need typed contracts; state needs explicit structure; termination needs limits; side effects need authorization/idempotency; failures need recovery paths.", "Use deterministic workflows when the sequence is known. Introduce model-driven routing only where ambiguity or intermediate results genuinely change the next action.", "Human-in-the-loop is not a failure. It is often the correct control for high-impact actions, low confidence, or policy exceptions."], "walkTitle": "Code walkthrough: bounded tool-using agent loop", "walkCode": "MAX_STEPS = 5\n\nfor step in range(MAX_STEPS):\n    decision = model_decide(goal, state, TOOL_SCHEMAS)\n\n    if decision[\"type\"] == \"final\":\n        return decision[\"answer\"]\n\n    call = validate_tool_call(decision)     # schema/business rules\n    authorize(user, call)                   # trusted policy\n    result = execute(call)                  # trusted runtime\n    state[\"history\"].append({\n        \"tool\": call[\"name\"],\n        \"result\": result\n    })\n\nraise RuntimeError(\"agent exceeded step budget\")\n", "steps": ["Step budget prevents infinite loops and unbounded cost.", "The model only emits a decision; validation and execution are separate.", "Authorization is evaluated every sensitive action, not once in the prompt.", "Observations are appended to explicit state so the next decision is based on verified results.", "Termination is part of agent design. A production agent needs a defined 'stop/ask human/abstain' path."], "runTitle": "Runnable mini-example: deterministic mock agent that chooses a calculator tool", "runCode": "def mock_model(goal, state, tools):\n    if not state[\"history\"]:\n        return {\"type\": \"tool\", \"name\": \"add\", \"args\": {\"a\": 2, \"b\": 3}}\n    return {\"type\": \"final\", \"answer\": f\"Result = {state['history'][-1]['result']}\"}\n\ndef execute(call):\n    if call[\"name\"] == \"add\":\n        return call[\"args\"][\"a\"] + call[\"args\"][\"b\"]\n    raise ValueError(\"unknown tool\")\n\nstate = {\"history\": []}\nfor _ in range(3):\n    d = mock_model(\"add 2 and 3\", state, [\"add\"])\n    if d[\"type\"] == \"final\":\n        print(d[\"answer\"]); break\n    result = execute(d)\n    state[\"history\"].append({\"tool\": d[\"name\"], \"result\": result})\n", "run": "Standard library only; the 'model' is mocked so you can study the control loop without an API key.", "expected": "`Result = 5`.", "drillQ": "What makes an agent different from a chatbot?", "outline": ["A chatbot may only generate a response.", "An agent is allowed to influence execution/control flow based on state and goals.", "That authority is bounded by tool schemas, policy, step/time/cost budgets, and trusted runtime execution.", "Framework choice is implementation detail."], "pitfalls": ["Calling any LLM application an agent.", "Using model reasoning to enforce permissions or irreversible business rules."]}, "security": {"title": "Security & Banking Controls — Deep Dive", "why": "In enterprise AI, the model must never be the security boundary. Authorization has to survive prompt injection, retrieval poisoning, and model error.", "mental": ["Authenticate the caller, resolve identity/entitlements, then enforce authorization at retrieval/tool/data boundaries. A model can propose an action but cannot grant access.", "User identity and service identity solve different problems. A service may access downstream systems with its own workload identity, while the application separately enforces user entitlements; delegated/on-behalf-of patterns propagate user context when downstream systems require it.", "RAG ACL metadata must be enforced before unauthorized chunks enter context. Filtering the final answer is too late because the model has already seen the data.", "Prompt injection is an untrusted-input problem. System prompts help behavior, but real containment comes from least-privilege tools, validation, data isolation, approval gates, and output controls."], "walkTitle": "Code walkthrough: entitlement-aware retrieval boundary", "walkCode": "def retrieve_for_user(user, query):\n    # user.entitlements might contain business lines/doc groups\n    filters = {\n        \"business_line\": {\"$in\": user.entitlements},\n        \"classification\": {\"$ne\": \"restricted\"}\n    }\n\n    # push authorization constraints into retrieval\n    return vector_store.search(\n        query=query,\n        filters=filters,\n        top_k=10\n    )\n", "steps": ["User entitlements are resolved by a trusted identity/authorization system, not inferred by the model.", "Filters are converted into datastore-level constraints.", "Unauthorized documents are never returned to context construction.", "The same principle should apply to tool resources and downstream data queries."], "runTitle": "Runnable mini-example: least-privilege document filtering", "runCode": "docs = [\n {\"id\": 1, \"line\": \"commercial\", \"text\": \"Commercial policy\"},\n {\"id\": 2, \"line\": \"wealth\", \"text\": \"Wealth policy\"},\n]\n\ndef allowed_docs(entitlements):\n    return [d for d in docs if d[\"line\"] in entitlements]\n\nprint(allowed_docs({\"commercial\"}))\n", "run": "Standard library only.", "expected": "Only the commercial document is returned.", "drillQ": "Where should authorization live in a RAG system?", "outline": ["At identity-aware retrieval/data boundaries, ideally pushed into the query/filter.", "Again at sensitive tool/action boundaries—defense in depth.", "Not solely in the prompt or after generation.", "Preserve audit records of subject, resource, action, decision, and trace/run ID."], "pitfalls": ["Assuming a system prompt is a security control.", "Using one broad database/service credential with no per-user entitlement enforcement."]}, "production": {"title": "Production Engineering — Deep Dive", "why": "A prototype proves possibility. Production engineering proves repeatable service quality under failures, changes, scale, and ownership.", "mental": ["Define SLOs and failure budgets: availability, latency, task success, retrieval quality, cost, and human escalation depending on the service.", "Version AI-specific components independently: model, prompt, tool schema, parser, chunker, embedding, index, eval set. Observability must record those versions per run.", "Separate infrastructure health from AI-quality health. HTTP 200 can still contain the wrong tool call or unsupported answer.", "Plan rollback/rebuild paths. Some changes can roll back instantly (prompt/model); others require reindexing (chunker/embedding/parser) and may need dual-index deployment."], "walkTitle": "Code walkthrough: release metadata attached to every run", "walkCode": "RELEASE = {\n    \"app\": \"2.7.0\",\n    \"model\": \"model-A\",\n    \"prompt\": \"plan-v12\",\n    \"tool_schema\": \"tools-v4\",\n    \"index\": \"policy-2026-08-28\",\n    \"embedding\": \"embed-v3\",\n}\n\ndef log_run(trace_id, metrics):\n    record = {\n        \"trace_id\": trace_id,\n        \"release\": RELEASE,\n        \"metrics\": metrics,\n    }\n    print(record)  # send to telemetry store in production\n", "steps": ["If quality regresses, you need to know which combination produced the run.", "Release metadata should travel with traces/eval results, not live only in deployment documentation.", "Index/embedding versions are especially important because rolling back the application alone may not restore old retrieval behavior.", "Metrics should include both platform and AI-quality signals."], "runTitle": "Runnable mini-example: simple latency + success telemetry", "runCode": "import time\n\nstart = time.perf_counter()\nok = True\n# ... execute request ...\nlatency_ms = (time.perf_counter() - start) * 1000\n\nlog_run(\"trace-123\", {\n    \"ok\": ok,\n    \"latency_ms\": round(latency_ms, 2),\n    \"tokens\": 1200,\n    \"tool_calls\": 2,\n})\n", "run": "Standard library only.", "expected": "A structured run record containing trace ID, component versions, and metrics.", "drillQ": "What changes when a RAG prototype becomes a production service?", "outline": ["Reliability: timeouts, retries, queues, checkpointing, idempotency.", "Security: identity, entitlements, secrets, audit, prompt-injection containment.", "Quality: versioned evals and release gates.", "Operations: observability, SLOs, alerts, on-call ownership, rollback.", "Economics: token/compute/index cost and capacity."], "pitfalls": ["Monitoring only service uptime.", "Treating prompt/model version as the only relevant AI release dimension."]}};
